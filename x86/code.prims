code{
bye halt
	hlt
}code

inner{
key key
	in al, dx	; read it
	mov bl, al	; save as a stashed value
}inner

inner{
emit emit
	mov al, bl	; restore stashed value
	out dx, al	; and echo it out
}inner

inner{
dup dup
	_dup
}inner

inner{
drop drop
	_drop
}inner

inner{
neg neg
	_neg
}inner

inner{
inv inv
	_inv
}inner
